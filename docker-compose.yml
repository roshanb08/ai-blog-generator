services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: newsblog-ai:latest
    container_name: newsblog-ai
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - dedup_data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      start_period: 20s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 128M

volumes:
  dedup_data:
    driver: local
